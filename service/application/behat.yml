default:
    extensions:
        Behat\MinkExtension:
            base_url: http://192.168.100.101/
            goutte: ~
        Behat\Symfony2Extension:
          kernel:
            env: "test"
            debug: "false"
        Behatch\Extension: ~

    suites:
        default:
            contexts:
                - FeatureContext
                - Behat\MinkExtension\Context\MinkContext

        login:
            paths: [ %paths.base%/features/login ]
            contexts:
                - behatch:context:rest
                - UserSetupContext:
                      userManagerService: "@user_service.core"
                - RestApiContext:
                      client: "@csa_guzzle.client.local_api_test"

                #- FeatureContext
                #- DefaultContext:
                #      fixtures:
                #          - %paths.base%/tests/Functional/Features/user.yml
                #      keepDatabase: true
                # - Behat\MinkExtension\Context\MinkContext


####  Further necessary conexts that could be used with any suite
            # contexts:
               # - behatch:context:browser
               # - behatch:context:debug
               # - behatch:context:system
               # - behatch:context:json
               # - behatch:context:table
               # - behatch:context:rest
               # - behatch:context:xml
